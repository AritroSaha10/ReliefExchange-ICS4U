/**
 * Data schema for a Donation Tag (ex. clothing, electronics). See more examples at ../tag-types.ts
 */
export default interface DonationTag {
    id: number,
    name: string,
    color: string
}